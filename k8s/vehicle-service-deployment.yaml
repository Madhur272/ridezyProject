apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicle-service
  labels:
    app: vehicle-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vehicle-service
  template:
    metadata:
      labels:
        app: vehicle-service
        tier: backend
    spec:
      containers:
        - name: vehicle-service
          image: ridezyproject-vehicle-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 4003
              name: http
          envFrom:
            - configMapRef:
                name: ridezy-config
            - secretRef:
                name: ridezy-dev-secrets
          env:
            - name: PORT
              value: "4003"
            - name: SERVICE_NAME
              value: vehicle-service
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 4003
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
