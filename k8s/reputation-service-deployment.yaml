apiVersion: apps/v1
kind: Deployment
metadata:
  name: reputation-service
  labels:
    app: reputation-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reputation-service
  template:
    metadata:
      labels:
        app: reputation-service
        tier: backend
    spec:
      containers:
        - name: reputation-service
          image: ridezyproject-reputation-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 4007
              name: http
          envFrom:
            - configMapRef:
                name: ridezy-config
            - secretRef:
                name: ridezy-dev-secrets
          env:
            - name: PORT
              value: "4007"
            - name: SERVICE_NAME
              value: reputation-service
          readinessProbe:
            httpGet:
              path: /health
              port: 4007
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 4007
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
