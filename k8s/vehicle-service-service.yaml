apiVersion: v1
kind: Service
metadata:
  name: vehicle-service
  labels:
    app: vehicle-service
spec:
  selector:
    app: vehicle-service
  ports:
    - name: http
      port: 4003
      targetPort: 4003
  type: ClusterIP
