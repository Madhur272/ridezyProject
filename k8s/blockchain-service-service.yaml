apiVersion: v1
kind: Service
metadata:
  name: blockchain-service
  labels:
    app: blockchain-service
spec:
  selector:
    app: blockchain-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002
  type: ClusterIP
