apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-service
  labels:
    app: iot-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iot-service
  template:
    metadata:
      labels:
        app: iot-service
        tier: backend
    spec:
      containers:
        - name: iot-service
          image: ridezyproject-iot-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 4012
              name: http
          envFrom:
            - configMapRef:
                name: ridezy-config
            - secretRef:
                name: ridezy-dev-secrets
          env:
            - name: PORT
              value: "4012"
            - name: SERVICE_NAME
              value: iot-service
          readinessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 4012
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
